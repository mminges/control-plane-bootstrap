credentials:
- name: /p-bosh/concourse/atc_tls
  type: certificate
  value:
    ca: |
      # drop a valid ca cert here
    certificate: |
      # drop a valid cert here
    private_key: |
      # drop a valid private key here
- name: /p-bosh/concourse/atc_ca
  type: certificate
  value:
    ca: |
      # drop a valid ca cert here
    certificate: |
      # drop a valid cert here (same as ca above)
